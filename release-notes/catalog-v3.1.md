# Catalog v3.1

Opus 4.8 alignment ship for the rootnode-skills system. 27 Skills recalibrated 2026-06-25.

This release coordinates the v3.1 catalog ship. Each Skill ships its own per-package release (`<skill>/v3.1`); this is the umbrella that frames the catalog as a unified system.

## What's in v3.1

The apply-now Opus 4.8 alignment pass across the full 27-Skill catalog, two surfaces (chat-side Claude Projects and Claude Code execution):

- **Voice/framing recalibration to Opus 4.8.** Reasoning-discipline scaffolding lightened to reasoned voice (gates preserved); advisory pipelines reframed to outcome anchors with within-stage gates intact. 12 Skills edited, 15 processed-untouched. No method, output, or gate changed in any Skill — methodology preservation held throughout.
- **`rootnode-session-handoff` v2.0.** Markdown handoff output, forced knowledge-file delta capture, completeness gate, carry-forward ledger. Cleared its behavioral case set via independent grader.
- **Model-version sweep.** Calibration references updated to Claude Opus 4.8 (primary), 4.6 (secondary).
- **Canonical knowledge-file sync.** AEA / CC_EG / SBD / OPT_REF brought current in `audit/canonical-kfs/`.

Per-Skill surface packaging is unchanged: 24 `-cp` (flat, for Claude.ai Projects upload) + 5 `-cc` (wrapper, for `~/.claude/skills/` install) across the 27 Skills; the 2 dual-surface Skills ship both.

## Deferred to v3.2 (Phase 4, test-gated)

- Tier-marker STRENGTHEN changes and "Opus recommended" caveat-drops, gated on regression testing.
- README compatibility-matrix and per-Skill tier-marker regeneration from validated results.

## Install

Download any `<skill>/v3.1` per-package release. `-cp` zips upload directly to Claude.ai (Settings → Capabilities → Skills); `-cc` zips extract into `~/.claude/skills/`.
